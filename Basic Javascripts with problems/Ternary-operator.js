// Ternary operator
var number1 = 1000;
var number2 = 2000;
//  var test = (condition) ? "true" : "false";
var result = (number1 > number2) ? "Number1 is big" : "Number2 is big";
//ekhane ? and : ei 2 ta condition er jonno use hoise

console.log(result);